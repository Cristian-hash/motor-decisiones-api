El Objetivo: Preparar tus herramientas de telemetría forense antes de que el Usuario 2 toque el teclado. No puedes auditar el estrés si no estás grabando los signos vitales del sistema.

Acción Paso a Paso:

La Bóveda: Abre DBeaver/pgAdmin y conéctate a tu Azure Database for PostgreSQL. Ejecuta DELETE FROM progresos; para limpiar el campo de batalla de ayer.

El Radar: Entra al portal de Azure (portal.azure.com). Ve a tu App Service (donde vive tu Spring Boot), busca la opción Log stream (Flujo de registros) en el menú lateral y déjalo abierto. Aquí verás los Exception y StackTrace en vivo.

El Perfil: Crea un nuevo usuario en tu base de datos o permite que el Usuario 2 se registre desde cero para que su Token JWT sea fresco.


Cree un nuevo usuario desde la Base de Datos.