sequenceDiagram
autonumber

    participant Controller as 🛎️ Controller
    participant Service as 🧠 EvaluacionService
    participant Interfaz as 🔌 EventPublisher<br/>(Interfaz)
    participant Adaptador as 🔌 KafkaEventPublisher<br/>(Adaptador)
    participant Kafka as 🌪️ Apache Kafka<br/>Topic: gamificacion-topic
    participant Consumer as 🎧 GamificacionKafkaConsumer<br/>@KafkaListener
    participant Logros as 🏆 LogrosService

    Controller->>Service: evaluarDecision(RespuestaEstudianteDTO)

    Note over Service: Regla Antifraude<br/>Evalúa Estrategia<br/>progresoRepository.save(nuevoProgreso)

    Service->>Service: new LeccionCompletadaEvent(<br/>usuarioId,<br/>leccionId,<br/>puntosGanados)

    Note over Service,Interfaz: El cerebro solo conoce el "enchufe ciego".<br/>Kafka vive fuera de esta capa.

    Service->>Interfaz: publicarLeccionCompletada(event)

    Interfaz->>Adaptador: Delega a la implementación real

    Note over Adaptador: Convierte el objeto Java a JSON

    Adaptador->>Adaptador: objectMapper.writeValueAsString(event)

    Adaptador->>Kafka: kafkaTemplate.send("gamificacion-topic", mensajeJson)

    Service-->>Controller: FeedbackDTO

    Note left of Service: ⚡ El hilo HTTP queda libre.<br/>Respuesta 200 OK al frontend.

    rect rgb(245,245,245)
    Note over Kafka,Consumer: Ruptura del Tiempo (Asincronía)
    end

    Kafka-->>Consumer: escucharEventoGamificacion(mensajeJson)

    Note over Consumer: @KafkaListener recibe el mensaje<br/>del grupo tesis-group

    Consumer->>Consumer: objectMapper.readValue(<br/>mensajeJson,<br/>LeccionCompletadaEvent.class)

    Note over Consumer,Logros: Entrega el evento reconstruido<br/>al especialista

    Consumer->>Logros: evaluarInsignias(usuarioId)

    Note over Logros: Lógica de negocio pura

    Logros->>Logros: if (puntosExperiencia >= 50)

    Logros->>Logros: usuario.setInsignia("Arquitecto Junior")<br/>usuarioRepository.save(usuario)