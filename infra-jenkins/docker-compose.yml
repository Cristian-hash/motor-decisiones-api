services:
  jenkins:
    image: jenkins/jenkins:lts
    container_name: jenkins-mayordomo
    user: root
    ports:
      - "8081:8080"
    volumes:
      - jenkins_data:/var/jenkins_home

volumes:
  jenkins_data: